# Pass 15 — gap analysis: техническое письмо ≠ ТЗ

Существующий `technical` mode сохраняется: он проверяет редакторские признаки технического текста. Для ТЗ добавлен orthogonal layer `check_tz_ru.py`, поскольку качество требования нельзя корректно свести к длине предложения, jargon density или ритму.

Границы: frozen linguistic policy 1.4.0 не меняется; ТЗ-правила имеют независимую версию 1.0.0; generic profile не заявляет соответствие ГОСТ; структурная ГОСТ-проверка включается только явно.
